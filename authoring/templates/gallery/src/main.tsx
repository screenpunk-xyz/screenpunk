import { useState } from 'react';
import { createRoot } from 'react-dom/client';
import { ScreenpunkProvider, useScreenReady } from '@screenpunk/react';
import { Activity, Button, Card, Carousel, Choice, DataTable, InfoDialog, Input, Reveal, TabGroup, TrendChart } from '@screenpunk/ui';
function Gallery(){useScreenReady();const [count,setCount]=useState(0);const [choice,setChoice]=useState('Day');return <main><div className="sp-badge">Screenpunk / component gallery</div><h1>Built for your screen.</h1><p className="sp-muted">Local components. No network required.</p><div className="sp-grid"><Card><h2><Activity aria-hidden/> Controls</h2><div className="sp-row"><Button onClick={()=>setCount(n=>n+1)}>Count {count}</Button><Choice label="Period" value={choice} onChange={setChoice} options={['Day','Week','Month']}/><InfoDialog title="Open dialog">Keyboard focus stays inside this dialog and returns to its trigger when closed.</InfoDialog><Input aria-label="Example input" placeholder="Type something"/></div></Card><Card><h2>Tabs</h2><TabGroup tabs={[{id:'one',label:'Overview',content:<p>Reusable UI, one theme.</p>},{id:'two',label:'Details',content:<p>Arrow keys move between tabs.</p>}]}/></Card></div><Card><TrendChart kind="bar" label="Sample trend" data={[{name:'Mon',value:4},{name:'Tue',value:7},{name:'Wed',value:5}]}/></Card><div className="sp-grid"><Card><h2>Sortable table</h2><DataTable data={[{name:'North',value:12},{name:'South',value:7}]} columns={[{accessorKey:'name',header:'Region'},{accessorKey:'value',header:'Value'}]}/></Card><Card><h2>Touch carousel</h2><Carousel>{[<p key="a">One finger moves this carousel.</p>,<p key="b">Two fingers switch Screenpunk screens.</p>]}</Carousel><Reveal><p>Motion respects reduced motion and inactivity.</p></Reveal></Card></div></main>}
createRoot(document.getElementById('root')!).render(<ScreenpunkProvider><Gallery/></ScreenpunkProvider>);
