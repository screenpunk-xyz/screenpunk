# Milestone 0 feasibility (not implemented)

Remaining after bootstrap:

1. Hidden AppKit/WKWebView helper returns a real screenshot over MCP.
2. Same page/bridge on iOS 16: native networking, no direct web egress,
   simulated content-process failure.
3. Authenticated LAN pairing fixtures (identity pin + matching code)
   before real credentials.

Do not postpone these until release week.
