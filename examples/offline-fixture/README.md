# Offline fixture

Minimal bundled page for later host tests. No upstream connections, so the
native Offline overlay must not appear from connection health.

Used by Milestone 0/1 isolation work. Not a designed first-party UI.
